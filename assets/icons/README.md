# Icons

- `jobtrack.ico` — Windows app icon (multi-resolution ICO, required for build.bat)
- `jobtrack.icns` — macOS app icon (required for build_mac.sh, future)
- `tray_icon.png` — System tray icon (32x32 PNG)
- Additional button/nav icons as needed by the UI
